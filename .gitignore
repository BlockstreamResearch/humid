# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
tmp/
storybook-static/

# Config files
.webextrc
.webextrc.*

# wfctl:begin
graphify-out/
# wfctl:end

# The stop guard's own state, which is per-session runtime bookkeeping rather than
# configuration. It lives beside the hook because `.workflow/current/` is where wfctl
# looks for work bindings and it parsed this as a malformed one.
.workflow/runtime/stop-guard.json
